package strings;

import java.util.*;

public class lengthcalculate {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("enter your full name=");
        String fullname = sc.nextLine();
        System.out.println("LENGTH OF FULL NAME ENTERED BY USER IS=" + fullname.length());
        sc.close();
        // Sting is a ready mail library function whose meaning is predefined in a
        // compiler.That's why we use its functions like length(),concatenating() or
        // copying() without writing any code.
        //Functions like length() concatenating() copying() is these are the redimed library functions.

    }
}
